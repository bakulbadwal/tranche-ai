{
  "name": "tranche-ai-review-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Off-chain milestone review agent for Tranche AI — reviews submitted evidence with Claude and posts an EAS attestation that the TrancheVault contract checks before releasing a tranche.",
  "type": "module",
  "scripts": {
    "review": "tsx --env-file=.env src/reviewMilestone.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@ethereum-attestation-service/eas-sdk": "^2.7.0",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
