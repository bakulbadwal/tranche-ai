# Set after deploying TrancheVault (and its ERC20) to a chain the frontend targets.
NEXT_PUBLIC_VAULT_ADDRESS=
NEXT_PUBLIC_TOKEN_ADDRESS=

# Optional: get a free project ID at cloud.walletconnect.com for production.
# Falls back to a dev-only placeholder if unset.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
